{
  "name": "@tkrotoff/fetch",
  "version": "0.0.4",
  "repository": {
    "type": "git",
    "url": "https://github.com/tkrotoff/fetch.git"
  },
  "description": "Fetch wrapper for JSON",
  "license": "MIT",
  "keywords": [
    "fetch",
    "json"
  ],
  "main": "dist/cjs/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/"
  ],
  "sideEffects": false,
  "scripts": {
    "clean": "rm -rf dist coverage",
    "clean:all": "npm run clean && rm -rf node_modules package-lock.json",
    "build:esnext": "tsc --noEmit false --outDir dist --declaration true",
    "build:cjs": "tsc --noEmit false --outDir dist/cjs --module commonjs",
    "build": "npm run clean && npm run build:esnext && npm run build:cjs",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "tsc": "tsc",
    "format": "prettier --write \"**/*\"",
    "lint": "eslint . '**/*.{js,ts}'",
    "precommit": "npm run format && tsc && npm run lint",
    "prepush": "npm run precommit && npm run test",
    "prepublishOnly": "npm run build",
    "publish:beta": "npm version 0.1.0-beta.1 && npm publish --tag next --dry-run"
  },
  "dependencies": {},
  "devDependencies": {
    "@babel/core": "^7.4.5",
    "@babel/preset-env": "^7.4.5",
    "@babel/preset-typescript": "^7.3.3",
    "@types/fetch-mock": "^7.3.0",
    "@types/jest": "^24.0.13",
    "@typescript-eslint/eslint-plugin": "^1.9.0",
    "@typescript-eslint/parser": "^1.9.0",
    "babel-jest": "^24.8.0",
    "eslint": "^5.16.0",
    "eslint-config-airbnb-base": "^13.1.0",
    "eslint-config-prettier": "^4.3.0",
    "eslint-plugin-import": "^2.17.2",
    "eslint-plugin-prettier": "^3.1.0",
    "fetch-mock": "^7.3.3",
    "husky": "^2.3.0",
    "jest": "^24.8.0",
    "node-fetch": "^2.6.0",
    "prettier": "^1.17.1",
    "typescript": "^3.4.5"
  }
}
